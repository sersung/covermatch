// Marketing layout — Navbar/Footer are in the root layout.
// This group is used for layout organization only.
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
